extends layout

block content
    div.full
        div.container12
            h1 Login
            p.lead
            | Sign in with your account. Don’t have one?
            a(href='/')  Register Now.
            form( action='/login', method='post')
                p
                    label(for='username') Username
                    input.form-control(type='text', id='user', name='email', placeholder='enter a username')
                p
                    label(for='password') Password
                    input.form-control(type='password', id='password', name='password', placeholder='enter a password')
                .buttonlogin
                    button.btns.btn-primary(type='submit') Submit
                    a.btn.btnc-default(role='button', href='/') Cancel